# SOMA - Self-Organizing Modular Architecture

🧬 Живое вычислительное тело, где каждая часть знает своё предназначение и развивается из общего кода.

## Структура

SOMA состоит из пяти основных модулей:

### 🧬 soma-core
Стволовые клетки и базовые интерфейсы системы.

- `Cell` trait - паттерн Sense-Align-Flow
- `StemCell` - дифференциация в специализированные типы
- Система резонансов для синхронизации состояния

### 🧠 soma-vnp
Виртуальные нейро/мышечные процессоры.

- `Neuron` - виртуальный нейрон с порогом активации
- `NeuronLayer` - слой связанных нейронов
- Временное затухание и накопление потенциала

### 🌉 soma-bridge
Связь с DAO/Garden и внешними модулями.

- `Transport` trait - абстракция транспортного слоя
- `Signal` - легковесная структура для передачи значений
- `Message` - полноценное сообщение с метаданными

### 🚪 soma-api
REST + WebSocket интерфейс и Node Mesh синхронизация.

- REST API для управления и мониторинга
- WebSocket для real-time потока сигналов
- **Node Mesh** - P2P синхронизация между узлами
- Handshake protocol для peer discovery
- Heartbeat мониторинг живых соединений
- State sync для распределённого резонанса
- CORS support для веб-клиентов

### 👁️ soma-mind
Inner Council - высокоуровневый контроль.

- **Пифия** (Pythia) - интуиция и предсказание
- **Морфей** (Morpheus) - сны и творчество
- **Архитектор** (Architect) - структура и планирование

### 🎬 soma-sim
Симулятор и визуализация.

- Отслеживание резонансов в реальном времени
- ASCII-визуализация состояния системы
- Статистика и экспорт в JSON

## Демонстрации

### Skeleton Demo
Показывает базовую структуру и взаимодействие всех модулей:

```bash
cargo run --release --bin soma-sim
```

### Pulse Demo ⚡
Визуализация живого пульса нейрона - накопление потенциала, активация и затухание:

```bash
cargo run --release --bin pulse
```

Вы увидите:
- 🔵 Голубой - низкий потенциал
- 🟢 Зеленый - средний уровень накопления
- 🟡 Желтый - близко к порогу активации
- 🔴 Красный - активация (fired) 🔥

### Resonance Demo 🌐
Два нейрона синхронизируются через канал связи, обнаруживая друг друга:

```bash
cargo run --release --bin resonance
```

Наблюдайте:
- Нейрон A (голубой/красный 🔥) и нейрон B (зеленый/фиолетовый ✨)
- ⚡ Резонанс появляется когда их состояния близки (> 0.8)
- Нейроны влияют друг на друга через Link
- Постепенная синхронизация ритмов

### Genesis Demo 🌍
Стволовой процессор порождает новые клетки в ответ на нагрузку системы:

```bash
cargo run --release --bin genesis
```

Наблюдайте:
- Нагрузка системы (Load) накапливается при высокой активности
- При превышении порога (0.5) стволовой процессор делится
- Рождаются новые клетки трёх типов: 🔵 Sensor, 🟢 Logic, 🟡 Motor
- Статистика каждые 20 циклов показывает распределение ролей
- Система балансирует количество клеток разных типов

### API Server 🚪
REST + WebSocket интерфейс для внешнего взаимодействия:

```bash
cargo run --release --bin soma-api
```

Доступные эндпоинты:
- `GET /` - Информация об API и Node Mesh
- `GET /state` - Состояние системы (клетки, поколение, нагрузка)
- `GET /cells` - Список всех клеток с деталями
- `GET /distribution` - Распределение ролей
- `GET /peers` - Список подключенных mesh узлов
- `POST /signal` - Отправить сигнал `{"id":"...", "value":0.5, "timestamp":...}`
- `POST /stimulate` - Стимулировать систему `{"activity":0.8}`
- `GET /ws` - WebSocket stream для real-time обновлений
- `GET /mesh` - WebSocket endpoint для P2P mesh соединений

Сервер запускается на `http://0.0.0.0:8080`

### Node Mesh 🌐
Запуск нескольких узлов для распределённой синхронизации:

```bash
# Запустить первый узел
NODE_ID=node_alpha PORT=8080 cargo run --release --bin soma-api

# В другом терминале - второй узел
NODE_ID=node_beta PORT=8081 cargo run --release --bin soma-api

# Подключить узлы через WebSocket
cargo run --release --bin connect_peer 8081 8080
```

Узлы автоматически обмениваются:
- **Handshake** - взаимное обнаружение (node_id, timestamp)
- **Heartbeat** - каждые 3 секунды для мониторинга
- **StateSync** - каждые 5 секунд (cells, generation, load)

Просмотр подключенных узлов:
```bash
curl http://localhost:8080/peers
```

## Сборка

```bash
# Собрать весь workspace
cargo build --release --workspace

# Запустить тесты
cargo test --workspace

# Проверить проект
cargo check --workspace
```

## Итерации

- **v0.1 Skeleton** - Организационный каркас, структура модулей
- **v0.2 Pulse** - Первое дыхание - живой нейрон с временной динамикой
- **v0.3 Resonance** - Два нейрона обнаруживают друг друга и синхронизируются
- **v0.4 Genesis** - Стволовой процессор порождает клетки с разными ролями
- **v0.5 API Layer** - REST + WebSocket интерфейс для внешнего взаимодействия
- **v0.6 Node Mesh** - Распределённая P2P синхронизация между узлами

Смотри [ROADMAP.md](ROADMAP.md) для планов будущих итераций (v0.7+).

## Архитектура

```
soma/
├── soma-core/       # Базовые абстракции (Cell, StemCell)
├── soma-vnp/        # Нейронные процессоры
├── soma-bridge/     # Транспортный слой
├── soma-mind/       # Inner Council
├── soma-sim/        # Симуляция и визуализация
└── soma-api/        # REST + WebSocket API
```

## Концепции

### Sense-Align-Flow
Базовый паттерн для всех клеток:
1. **Sense** - восприятие входящего сигнала
2. **Align** - внутреннее выравнивание/обработка
3. **Flow** - генерация выходящего потока

### Резонанс
Нормализованное значение (0.0 - 1.0) для передачи состояния между компонентами.

### Дифференциация
Стволовые клетки могут трансформироваться в специализированные типы, передавая начальное состояние.

## Лицензия

MIT OR Apache-2.0
