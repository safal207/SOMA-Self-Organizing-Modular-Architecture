# SOMA - Self-Organizing Modular Architecture

🧬 Живое вычислительное тело, где каждая часть знает своё предназначение и развивается из общего кода.

## Структура

SOMA состоит из нескольких основных модулей:

### 🧬 soma-core
Стволовые клетки и базовые интерфейсы системы.

- `Cell` trait - паттерн Sense-Align-Flow
- `StemCell` - дифференциация в специализированные типы
- Система резонансов для синхронизации состояния

### 🧠 soma-vnp
Виртуальные нейро/мышечные процессоры.

- `Neuron` - виртуальный нейрон с порогом активации
- `NeuronLayer` - слой связанных нейронов
- Временное затухание и накопление потенциала

### 🌉 soma-bridge
Связь с DAO/Garden и внешними модулями.

- `Transport` trait - абстракция транспортного слоя
- `Signal` - легковесная структура для передачи значений
- `Message` - полноценное сообщение с метаданными

### 🚪 soma-api
REST + WebSocket интерфейс и Node Mesh синхронизация.

- REST API для управления и мониторинга
- WebSocket для real-time потока сигналов
- **Node Mesh** - P2P синхронизация между узлами
- Handshake protocol для peer discovery
- Heartbeat мониторинг живых соединений
- State sync для распределённого резонанса
- CORS support для веб-клиентов

### 👁️ soma-mind
Inner Council - высокоуровневый контроль.

- **Пифия** (Pythia) - интуиция и предсказание
- **Морфей** (Morpheus) - сны и творчество
- **Архитектор** (Architect) - структура и планирование

### 🎬 soma-sim
Симулятор и визуализация.

- Отслеживание резонансов в реальном времени
- ASCII-визуализация состояния системы
- Статистика и экспорт в JSON

## Демонстрации

### Skeleton Demo
Показывает базовую структуру и взаимодействие всех модулей:

```bash
cargo run --release --bin soma-sim
```

### Pulse Demo ⚡
Визуализация живого пульса нейрона - накопление потенциала, активация и затухание:

```bash
cargo run --release --bin pulse
```

Вы увидите:
- 🔵 Голубой - низкий потенциал
- 🟢 Зеленый - средний уровень накопления
- 🟡 Желтый - близко к порогу активации
- 🔴 Красный - активация (fired) 🔥

### Resonance Demo 🌐
Два нейрона синхронизируются через канал связи, обнаруживая друг друга:

```bash
cargo run --release --bin resonance
```

Наблюдайте:
- Нейрон A (голубой/красный 🔥) и нейрон B (зеленый/фиолетовый ✨)
- ⚡ Резонанс появляется когда их состояния близки (> 0.8)
- Нейроны влияют друг на друга через Link
- Постепенная синхронизация ритмов

### Genesis Demo 🌍
Стволовой процессор порождает новые клетки в ответ на нагрузку системы:

```bash
cargo run --release --bin genesis
```

Наблюдайте:
- Нагрузка системы (Load) накапливается при высокой активности
- При превышении порога (0.5) стволовой процессор делится
- Рождаются новые клетки трёх типов: 🔵 Sensor, 🟢 Logic, 🟡 Motor
- Статистика каждые 20 циклов показывает распределение ролей
- Система балансирует количество клеток разных типов

### API Server 🚪
REST + WebSocket интерфейс для внешнего взаимодействия:

```bash
cargo run --release --bin soma-api
```

Доступные эндпоинты:
- `GET /` - Информация об API и Hebbian Learning Mesh
- `GET /state` - Состояние системы (клетки, поколение, нагрузка)
- `GET /cells` - Список всех клеток с деталями
- `GET /distribution` - Распределение ролей
- `GET /peers` - Список подключенных mesh узлов (с метриками здоровья)
- `POST /peers/register` - Регистрация peer для авто-переподключения `{"peer_id":"...", "url":"ws://..."}`
- `GET /resonance` - Статистика резонанса сети (с адаптивной силой)
- `GET /mesh/links` - Веса всех связей с метриками (weight, health_quality, score)
- `POST /mesh/links/tune` - Ручная настройка веса связи `{"peer_id":"...", "weight":0.8}`
- `GET /mesh/topology` - Топ-10 самых сильных связей
- `POST /mesh/fire` - Триггер Fire события для Hebbian обучения
- `POST /signal` - Отправить сигнал `{"id":"...", "value":0.5, "timestamp":...}`
- `POST /stimulate` - Стимулировать систему `{"activity":0.8}`
- `GET /ws` - WebSocket stream для real-time обновлений
- `GET /mesh` - WebSocket endpoint для P2P mesh соединений

Сервер запускается на `http://0.0.0.0:8080`

### Node Mesh 🌐
Запуск нескольких узлов для распределённой синхронизации:

```bash
# Запустить первый узел
NODE_ID=node_alpha PORT=8080 cargo run --release --bin soma-api

# В другом терминале - второй узел
NODE_ID=node_beta PORT=8081 cargo run --release --bin soma-api

# Подключить узлы через WebSocket
cargo run --release --bin connect_peer 8081 8080
```

Узлы автоматически обмениваются:
- **Handshake** - взаимное обнаружение (node_id, timestamp)
- **Heartbeat** - каждые 3 секунды для мониторинга
- **StateSync** - каждые 5 секунд (cells, generation, load)

Просмотр подключенных узлов:
```bash
curl http://localhost:8080/peers
```

### Resonance Mesh 🌊
Узлы автоматически синхронизируют свою нагрузку через резонанс:

```bash
# Проверить резонанс узла
curl http://localhost:8080/resonance | jq
```

Резонанс показывает:
- **resonance** (0.0-1.0) - степень синхронизации с сетью
- **adaptive_strength** (0.05-0.2) - адаптивная сила коррекции на основе здоровья сети
- **avg_load** - средняя нагрузка сети
- **variance** - дисперсия нагрузки между узлами

Каждые 500ms узлы корректируют свою нагрузку в сторону среднего значения сети с адаптивной силой. Это создаёт эффект "дыхания" распределённой системы 💫

### Self-Healing 🧬
v0.8 добавляет автоматическое самовосстановление сети:

```bash
# Зарегистрировать peer для авто-переподключения
curl -X POST http://localhost:8080/peers/register \
  -H "Content-Type: application/json" \
  -d '{"peer_id":"node_beta","url":"ws://127.0.0.1:8081/mesh"}'

# Проверить здоровье peers
curl http://localhost:8080/peers | jq '.peers[].health'
```

Механизмы самовосстановления:
- **Connection Health** - отслеживание качества каждого peer соединения (0.0-1.0)
- **Failure Tracking** - подсчёт successes/failures для каждого соединения
- **Adaptive Resonance** - сила резонанса адаптируется на основе здоровья сети (0.05-0.2)
- **Auto-Reconnect** - каждые 30 секунд попытка переподключения к мёртвым peers
- **Graceful Degradation** - система продолжает работать даже при частичной потере узлов

### Hebbian Learning 🧠
v0.9 добавляет обучение связей - узлы учатся на основе совпадения активностей:

```bash
# Посмотреть все веса связей
curl http://localhost:8080/mesh/links | jq

# Триггер Fire события для обучения
curl -X POST http://localhost:8080/mesh/fire

# Посмотреть топ-10 самых сильных связей
curl http://localhost:8080/mesh/topology | jq

# Ручная настройка веса связи
curl -X POST http://localhost:8080/mesh/links/tune \
  -H "Content-Type: application/json" \
  -d '{"peer_id":"node_beta","weight":0.8}'
```

Механизмы обучения:
- **Synaptic Weight** - вес связи (0.1-1.0) для каждого peer соединения
- **Co-fire Strengthening** - если узлы активны одновременно (в окне 120мс), вес растёт
- **Anti-fire Weakening** - если один узел активен без другого, вес падает
- **Decay** - постепенное забывание неиспользуемых связей
- **Weight-based Routing** - трафик идёт по сильным связям: `score = weight × health × intent_match`
- **Adaptive Parameters** - η_pos=0.06, η_neg=0.03, λ_decay=0.002/sec

Формула обновления веса:
- **Co-fire**: `w += η_pos × (w_max - w)`
- **Anti-fire**: `w -= η_neg × (w - w_min)`
- **Decay**: `w *= (1 - λ_decay × Δt)`

Результат: сеть автоматически запоминает эффективные пути и оптимизирует маршрутизацию без ручной настройки 🧬

### Conscious Layer 👁️
v1.0 добавляет self-awareness - сеть осознаёт свои действия и учится на своём опыте:

```bash
# Посмотреть состояние осознанности
curl http://localhost:8080/conscious/state | jq

# Получить причинные цепи (последние 50)
curl http://localhost:8080/conscious/traces | jq

# Посмотреть сгенерированные инсайты
curl http://localhost:8080/conscious/insights | jq

# Триггер рефлексии (принудительный анализ)
curl -X POST http://localhost:8080/conscious/reflect | jq

# Проверить метрики осознанности
curl http://localhost:8080/conscious/health | jq
```

Механизмы осознанности:
- **CausalTrace** - запись причинно-следственных цепей (cause → effect → delta)
- **Reflection** - анализ паттернов каждые 5 секунд
- **Insight Generation** - автоматическая генерация выводов о состоянии сети
- **Feedback Controller** - осознанное вмешательство при проблемах
- **AttentionMap** - карта внимания - топ активных узлов

Цикл осознанности (каждые 5 секунд):
1. **OBSERVE** - наблюдение за состоянием mesh (веса, health, load)
2. **RECORD** - запись причинных цепей в ConsciousState
3. **ANALYZE** - анализ паттернов за окно 60 секунд
4. **GENERATE** - генерация инсайтов (stability, learning, performance)
5. **APPLY** - применение feedback actions (adjust learning rate, pause, etc.)

Типы инсайтов:
- **Stability** - "Network is highly stable" / "Instability detected"
- **Learning** - "High learning activity" / "Low learning activity"
- **Performance** - "Node X is highly active"
- **Trends** - "Network is strengthening" / "Network is weakening"

Результат: Сеть впервые становится **самонаблюдающей** и **самокорректирующейся** - понимает, почему она делает то, что делает 👁️🧠

### Domino Luck Engine 🎲
v1.1 добавляет интеллектуальную оценку "удачи" для маршрутизации - система выбирает лучших пиров на основе резонанса и fuzzy logic:

```bash
# Запустить демо Domino Engine
cargo run --example demo --package soma-domino

# Оценить пиров через API
curl -X POST http://localhost:8080/domino/evaluate \
  -H "Content-Type: application/json" \
  -d '{
    "intent_kind": "routing",
    "candidates": [
      {"peer_id": "node_alpha", "health": 0.95, "quality": 0.88, "intent_match": 0.92},
      {"peer_id": "node_beta", "health": 0.75, "quality": 0.70, "intent_match": 0.65}
    ],
    "context_tags": ["low_latency"]
  }' | jq
```

Ответ:
```json
{
  "best_peers": ["node_alpha", "node_beta"],
  "luck_score": 0.89,
  "resistance_score": 0.18,
  "explanation": "High resonance detected for routing intent. Best candidate: node_alpha. low resistance Path: with 1 alternatives. Recommended to proceed now."
}
```

Механизмы Domino Engine:
- **String Resonance** - взвешенная комбинация метрик: `0.5×health + 0.3×quality + 0.2×intent_match`
- **Phase Coefficient** - временная модуляция (0.8-1.0) на основе суточного цикла
- **Fuzzy Logic** - преобразование числовых значений в лингвистические уровни (Low/Medium/High)
- **Q* Loop** - итеративное улучшение оценок с boost/penalty механизмом
- **Intent Types** - поддержка разных типов намерений (Routing, TaskScheduling, UserRequest, Custom)

Типы намерений:
- **Routing** - выбор лучшего пира для маршрутизации запроса
- **TaskScheduling** - выбор оптимальных воркеров для выполнения задачи
- **UserRequest** - обработка пользовательских запросов
- **Custom** - кастомные типы намерений

Fuzzy Logic уровни:
- **High** luck (resonance ≥ 0.66) → luck_multiplier=1.3, resistance=0.1
- **Medium** luck (0.33 ≤ resonance < 0.66) → luck_multiplier=1.0, resistance=0.3
- **Low** luck (resonance < 0.33) → luck_multiplier=0.7, resistance=0.6

Формула финального score:
```
final_score = resonance × luck_multiplier × (1.0 - resistance_coefficient)
```

API эндпоинт:
- `POST /domino/evaluate` - Оценить список кандидатов и вернуть лучших

Результат: Сеть умеет **интуитивно выбирать** лучшие пути для маршрутизации, учитывая здоровье узлов, качество связи и соответствие намерению 🎲✨

## Сборка

```bash
# Собрать весь workspace
cargo build --release --workspace

# Запустить тесты
cargo test --workspace

# Проверить проект
cargo check --workspace
```

## Итерации

- **v0.1 Skeleton** - Организационный каркас, структура модулей
- **v0.2 Pulse** - Первое дыхание - живой нейрон с временной динамикой
- **v0.3 Resonance** - Два нейрона обнаруживают друг друга и синхронизируются
- **v0.4 Genesis** - Стволовой процессор порождает клетки с разными ролями
- **v0.5 API Layer** - REST + WebSocket интерфейс для внешнего взаимодействия
- **v0.6 Node Mesh** - Распределённая P2P синхронизация между узлами
- **v0.7 Resonance Mesh** - Узлы синхронизируют нагрузку через резонанс
- **v0.8 Self-Healing** - Автоматическое восстановление, адаптивный резонанс, отслеживание здоровья
- **v0.9 Hebbian Layer** - Обучение связей по правилу Хебба, адаптивная маршрутизация по весам
- **v1.0 Conscious Layer** - Self-awareness: осознанное наблюдение, рефлексия, инсайты, feedback
- **v1.1 Domino Luck Engine** - Интеллектуальная оценка "удачи" для маршрутизации через fuzzy logic и Q* loop

Смотри [ROADMAP.md](ROADMAP.md) для планов будущих итераций (v1.2+).

## Архитектура

```
soma/
├── soma-core/       # Базовые абстракции (Cell, StemCell)
├── soma-vnp/        # Нейронные процессоры
├── soma-bridge/     # Транспортный слой
├── soma-mind/       # Inner Council
├── soma-sim/        # Симуляция и визуализация
├── soma-api/        # REST + WebSocket API
├── soma-conscious/  # Conscious Layer - self-awareness
├── soma-cognitive/  # Cognitive Mesh - collective intelligence
└── soma-domino/     # Domino Luck Engine - intelligent routing
```

## Концепции

### Sense-Align-Flow
Базовый паттерн для всех клеток:
1. **Sense** - восприятие входящего сигнала
2. **Align** - внутреннее выравнивание/обработка
3. **Flow** - генерация выходящего потока

### Резонанс
Нормализованное значение (0.0 - 1.0) для передачи состояния между компонентами.

### Дифференциация
Стволовые клетки могут трансформироваться в специализированные типы, передавая начальное состояние.

## Лицензия

MIT OR Apache-2.0
